
In the project directory, you can run:

### `npm start`

To run the application, you need to clone the repository and install the packages (npm install).

You also need to create an account on the website: https://rebrickable.com/minifigs/harry-potter/ and Generate an API Key.

Then add the .env file in the root directory and based on the .env.template use your key.
